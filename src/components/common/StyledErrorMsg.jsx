import styled from 'styled-components';

const StyledErrorMsg = styled.span`
  color: red;
  font-size: 0.8rem;
  height: 1.2rem;
  margin: 0;
  padding-left: 0.3rem;
`;

export default StyledErrorMsg;
